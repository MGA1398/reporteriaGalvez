{
  "scripts": {
    "start": "npm run dev",
    "frontend": "cd AngularApp && npm install && ng serve",
    "api": "cd CRUD_Pets && npm install && node index.js",
    "dev": "npm run api && npm run frontend",
    "heroku-postbuild": "cd CRUD_Pets && npm install"
  }
}
